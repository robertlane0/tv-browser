<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILD VARIANT ONLY — never shipped in release APKs.
  Extends the main policy with loopback exceptions so emulator QA can serve
  test pages over adb reverse. Release keeps cleartext forbidden globally
  except per-domain entries logged in specs/11-security-privacy-and-drm.md §6.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
